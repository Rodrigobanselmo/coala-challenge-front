import { ElementType, ReactNode } from "react";

import { Theme, TypographyProps } from "@mui/material";
import { SxProps } from "@mui/system";

export interface SPageTitleProps extends TypographyProps {
  icon: ElementType<any>;
}
